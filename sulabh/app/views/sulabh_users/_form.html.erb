<%= form_for(@sulabh_user) do |f| %>
  <% if @sulabh_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sulabh_user.errors.count, "error") %> prohibited this sulabh_user from being saved:</h2>

      <ul>
      <% @sulabh_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password_id %><br>
    <%= f.text_field :password_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
