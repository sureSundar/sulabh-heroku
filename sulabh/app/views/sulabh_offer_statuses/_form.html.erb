<%= form_for(@sulabh_offer_status) do |f| %>
  <% if @sulabh_offer_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sulabh_offer_status.errors.count, "error") %> prohibited this sulabh_offer_status from being saved:</h2>

      <ul>
      <% @sulabh_offer_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :offer_id %><br>
    <%= f.text_field :offer_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
