<%= form_for(@sulabh_role) do |f| %>
  <% if @sulabh_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sulabh_role.errors.count, "error") %> prohibited this sulabh_role from being saved:</h2>

      <ul>
      <% @sulabh_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
