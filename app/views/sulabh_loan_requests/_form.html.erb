
	<%= form_for(@sulabh_loan_request,:html => { :class => "form-signin" }) do |f| %>
	  <% if @sulabh_loan_request.errors.any? %>
		<div id="error_explanation">
		  <h2><%= pluralize(@sulabh_loan_request.errors.count, "error") %> prohibited this sulabh_loan_request from being saved:</h2>

		  <ul>
		  <% @sulabh_loan_request.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		  <% end %>
		  </ul>
		</div>
	  <% end %>  

	  <div class="field">
		<%= f.label "Requestor" %><br/>
		<%= f.hidden_field :sulabh_user_profile_id, :value => SulabhUserProfile.where(:mobile => current_user.phoneno)[0].id %>
		<%= SulabhUserProfile.where(:mobile => current_user.phoneno)[0].name %>
	  </div>
	  <div class="field">
		<%= f.label "Behaviour Score" %>
		<%= f.text_field :behavescore,:class=> "form-control",:placeholder => "Behaviour Score",:readonly => "true"  %>
	  </div>
	  <div class="field">
		
		<%= f.text_field :amount,:class=> "form-control",:placeholder => "Amount" %>
	  </div>
	  <div class="field">
		
		<%= f.text_field :requiredby , :id=> "requiredby_fld",:class=> "form-control",:placeholder => "Required By Date"  %>
	  </div>
	  <div class="field">
		
		<%= f.text_field :paybydate , :id=> "paybydate_fld" ,:class=> "form-control", :placeholder => "Pay by Date" %>
	  </div>
	  <div class="field">
		
		<%= f.text_field :mininterest,:class=> "form-control", :placeholder => "Minimum Interest" %>
	  </div>
	  <div class="field">
		
		<%= f.text_field :maxinterest,:class=> "form-control",:placeholder => "Maximum Interest"  %>
	  </div>
	  <!--div class="field">
		<%= f.label :interest %><br>
		<%= f.text_field :interest %>
	  </div>
	  <div class="field">
		<%= f.label :minpaybyamount %><br>
		<%= f.text_field :minpaybyamount %>
	  </div>
	  <div class="field">
		<%= f.label :maxpaybyamount %><br>
		<%= f.text_field :maxpaybyamount %>
	  </div -->
	  <div class="actions">
		<%= f.submit :class=> "btn btn-primary btn-md btn-block"%>
	  </div>
	<% end %>
<script>
var dateToday = new Date();
var dates = $("#requiredby_fld").datepicker({
	dateFormat: "yy-mm-dd",
    defaultDate: "+1w",
	maxDate: "+1m",
    changeMonth: true,
    //numberOfMonths: 3,
    minDate: dateToday
    /*
    onSelect: function(selectedDate) {
        var option = this.id == "from" ? "minDate" : "maxDate",
            instance = $(this).data("datepicker"),
            date = $.datepicker.parseDate(instance.settings.dateFormat || $.datepicker._defaults.dateFormat, selectedDate, instance.settings);
        dates.not(this).datepicker("option", option, date); 
    }*/
});
var dates1 = $("#paybydate_fld").datepicker({
    defaultDate: "+1w",
    dateFormat: "yy-mm-dd",
	maxDate: "+12m",
    changeMonth: true,
    //numberOfMonths: 3,
    minDate: dateToday
    /*
    onSelect: function(selectedDate) {
        var option = this.id == "from" ? "minDate" : "maxDate",
            instance = $(this).data("datepicker"),
            date = $.datepicker.parseDate(instance.settings.dateFormat || $.datepicker._defaults.dateFormat, selectedDate, instance.settings);
        dates1.not(this).datepicker("option", option, date); 
    } 
    */
});

</script>
